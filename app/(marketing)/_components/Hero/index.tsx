import Bounded from '../Bounded'
import AnimatedContent from './AnimatedContent'

const Hero = () => {
  return (
    <Bounded className="text-center">
      <AnimatedContent />
    </Bounded>
  )
}

export default Hero
